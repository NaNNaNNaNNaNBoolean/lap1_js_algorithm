
//1 capitalise function

function capitalise(word) {
    //.toUpperCase()
    wordLower = word.toLowerCase();
    let firstLetter = word[0].toUpperCase();
    
    return console.log(capitalizedWord = firstLetter + wordLower.slice(1));
}
capitalise("hEllo")
capitalise("avGeroU")
