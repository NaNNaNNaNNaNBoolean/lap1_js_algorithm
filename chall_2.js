
//2 swap the case function

const swapcase =word =>{
    let new_word = "";
    for (let i = 0; i < word.length; i++){
        tolowercase()
        if (word[i] === word[i].toLowerCase()){
            new_word += word[i].toUpperCase();
        }else{ new_word += word[i].toLowerCase()}
    }
      return console.log(new_word);
  } 

swapcase("JournAl") 
swapcase("lAfOSSEaCADEMY") 
